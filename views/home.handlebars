<main>
    {{#each posts as |post|}}
        <div>
            <div>
                <h2>{{title}}</h2>
                <h3>post by {{user.username}} on {{createdAt}}</h3>
            </div>
            <div>
                <p>{{text}}</p>
            </div>
        </div>
    {{/each}}
</main>